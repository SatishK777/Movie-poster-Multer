<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie Flex</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Set the background image for the body */
    
    body {
      
      background: url('/uploads/movie-2.jpg');
      background-size: cover;
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
     
    }

    /* Create a blur effect for the form container */
    .form-container {
      background: rgba(255, 255, 255, 0.1); /* Semi-transparent white background */
      backdrop-filter: blur(10px); /* Apply blur effect */
      border-radius: 15px;
      padding: 30px;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Soft shadow for depth */
    }

    /* Style the header */
    header {
      background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent black background */
      padding: 10px;
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
    }

    header nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
      text-align: center;
    }

    header nav ul li {
      display: inline-block;
      margin: 0 15px;
    }

    header nav ul li a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    /* Style the button */
    button {
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      margin-top: 15px;
    }

    button:hover {
      background-color: #45a049;
    }

    /* Center content and add some spacing at the top for the fixed header */
    .container {
      padding-top: 80px;
    }

    /* Add styles for input fields */
    input[type="text"],
    input[type="date"],
    input[type="number"],
    textarea,
    input[type="file"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
    }

    /* Style the card body to fit within the form container */
    .card-body {
      padding: 0;
    }
  </style>
</head>

<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/add">Add Movie</a></li>
      </ul>
    </nav>
  </header>

  <div class="container">
    <h1 class="text-center mb-4 fw-bold ">Add New Movie</h1>
    <div class="form-container">
      <div class="card-body">
        <form action="/add" method="POST" enctype="multipart/form-data">
          <div>
            <input type="text" id="title" name="title" placeholder="Name" required>
          </div>
          <div>
            <textarea id="description" name="description" placeholder="Description" required></textarea>
          </div>
          <div>
            <input type="date" id="releaseDate" name="releaseDate" required>
          </div>
          <div>
            <input type="text" id="genre" name="genre" placeholder="Genre" required>
          </div>
          <div>
            <input type="number" id="rating" name="rating" placeholder="Rating"c step="0.1" min="0" max="10" required>
          </div>
          <div>
            <input type="file" id="poster" name="poster" required>
          </div>
          <button type="submit">Add Movie</button>
        </form>
      </div>
    </div>
  </div>
</body>

</html>
